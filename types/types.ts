export type ServicesProps = {
    title: string,
    image: string,
    slug: string 
  }

  export type RegisterInputProps = {
    firstName: string,
    lastName: string,
    email: string,
    password: string
  }

